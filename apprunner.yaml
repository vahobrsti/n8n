version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install -g pnpm
    build: 
      #- pnpm install --frozen-lockfile
			- find / -name pnpm.cjs 
	    - npm prefix -g
      - pnpm run build
run:
  command: pnpm run start
  network: 
    port: 5678
    env: PORT
