#- pnpm install --frozen-lockfile
version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install --save pnpm 
      - npm install
    build: 
      - pnpm run build
run:
  command: pnpm run start
  network: 
    port: 5678
    env: PORT
