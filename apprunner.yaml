version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install -g pnpm
    build: 
      #- pnpm install --frozen-lockfile
      - pnpm run build
run:
  command: pnpm run start
  network: 
    port: 5678
    env: PORT
