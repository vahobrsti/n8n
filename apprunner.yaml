#- pnpm install --frozen-lockfile
version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - chmod +x init.sh
      - ./init.sh
    build:
      - pnpm run build
run:
  command: pnpm run start
  network:
    port: 5678
    env: PORT
